# 2026-02-19 — Durable memories

## Model Updates & Session Debugging
- **12:10 UTC**: User requested switching to `deepseek-v3.2` and adding GPT‑5.2/5.1 Codex to allowlist. Applied `gateway.config.patch` to agent main's fallbacks: added `openai-codex/gpt-5.2-codex`, `openrouter/deepseek/deepseek-v3.2`, `openrouter/minimax/minimax-m2.1`. Session switched to DeepSeek v3.2.
- **Attempt to switch to GPT‑5.1 Codex** failed with "Model not allowed" (likely account access issue). GPT‑5.2 Codex works.

## Cron Jobs Post‑Mortem & Channel Reorganization
- **Morning failures**: `daily-morning-briefing`, `Twitter RSS Digest`, `qubika-deadline-check` all failed with `cron announce delivery failed`. Root cause likely **context window exhaustion** (briefing used ~79k tokens) leading to `HTTP 401: User not found`.
- **Decision**: Move personal/individual cron outputs to **Telegram groups**; keep Brokia/ops alerts in Slack.
- **Telegram groups created** (IDs provided):
  - `Clawdius - Qubika`: `-5218900004`
  - `Calwdius - Newsletters`: `-5295394319`
  - `Calwdius - Finance`: `-5042828486`
  - `Calwdius - Twitter`: `-5095009832`
  - `Calwdius - Briefings`: `-5248435389`
  - `Calwdius - Music Radar`: `-5291576044`
- **Slack channels** remain:
  - `#brokia-ai-alerts` → only Brokia calendar alerts.
  - `C0AFK08BFR8` → ops/healthcheck alerts (nightly summary, security audit, update status).
- **Healthcheck jobs** updated to target Slack `C0AFK08BFR8` (both `healthcheck:security-audit` and `healthcheck:update-status`).

## Brokia Calendar Alert Fix
- **14:30 UTC**: Updated `brokia-calendar-alerts-daily` payload to **exclude** "Feria de Proyectos" (not relevant for entrepreneurship thesis) and **prioritize** "Taller de Proyecto Emprendedor" (starts 24 Feb).
- Test run executed; user asked to verify Slack output.

## Pending Actions
- **Update cron jobs** with new Telegram group IDs (still pending for many jobs).
- **Reduce token load** on `daily-morning-briefing` to avoid context‑window errors.
- **Monitor** next Twitter Digest run (scheduled 08:21 UTC) after delivery change to Telegram DM (temporary).
- **Create skill** `model-admin` for safe model‑switching and config‑patch workflows.

## Configuration Notes
- `CHANNELS.md` created to store Telegram group mappings.
- `plans/telegram-groups-migration.md` outlines the full migration plan.

## Lessons
- Context‑window exhaustion can cause `401 User not found` in cron delivery; need to split heavy jobs or reduce input tokens.
- Telegram groups are effective for separating notification streams; Slack reserved for team‑facing alerts.