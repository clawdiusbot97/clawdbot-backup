# Bitácora Brokia — 2026-02-21

## Resumen ejecutivo
Durante la jornada se implementaron mejoras estructurales en la infraestructura de automatizaciones para reducir el crecimiento de contexto y costos de modelos. Se desarrolló un MVP de Tool Output Compaction (wrapper `exec_compact`) que compacta outputs extensos guardando raw en artefactos e inyectando solo resúmenes al contexto. Además, se reestructuró el flujo del morning briefing en dos cron jobs separados (fetch de fuentes y ensamblado/envió) para evitar timeouts y mejorar robustez. Se corrigieron errores de formato en jobs existentes y se ajustaron umbrales de timeout tras una auditoría de rendimiento del gateway.

## Alcance del día
Se revisaron y modificaron componentes vinculados a:
- Implementación de Tool Output Compaction MVP (wrapper para `exec`).
- Reestructuración del cron job `daily-morning-briefing-telegram` en dos jobs independientes.
- Corrección de errores de malformación en comandos `read_compact.sh`.
- Auditoría de rendimiento del gateway y ajuste de timeouts.
- Actualización de jobs cron existentes para usar wrappers compactadores (`exec_compact.sh`, `read_compact.sh`).
- Definición de mejoras estructurales futuras: Session Rotation, Cron Cost Guard, validación de métricas.

## Cambios y evidencia (trazabilidad)

### 1) Tool Output Compaction MVP
- **Fuente:** Solicitud de usuario el 2026‑02‑21 19:32 UTC (memoria `2026‑02‑21‑2035.md`).
- **Objetivo:** Reducir tokens inyectados al contexto cuando outputs de tools superan 150 líneas o 10k caracteres.
- **Implementación:** 
  - Skill `exec‑compact` creado en `/home/manpac/.openclaw/workspace/skills/exec‑compact/`.
  - Wrapper script `exec_compact.sh` que intercepta `exec`, guarda raw output en `artifacts/raw/exec/<timestamp>.log`, genera resumen ≤300 tokens con bullets y líneas clave, e inyecta solo el resumen.
  - Umbrales: >150 líneas **o** >10k caracteres.
- **Evidencia:** 
  - Script principal: `/home/manpac/.openclaw/workspace/skills/exec‑compact/scripts/exec_compact.sh`
  - SKILL.md: descripción y parámetros.
  - Tests 1‑3 PASS confirmados por usuario (20:17 UTC).
- **Impacto:** Primer paso para reducir avg tokens/msg de ~51k a <12k meta.

### 2) Reestructuración del morning briefing (split en dos jobs)
- **Fuente:** Decisión operativa el 2026‑02‑22 00:02 UTC (memoria `2026‑02‑22‑0017.md`).
- **Motivación:** Evitar timeouts del gateway (30s) y separar fetch de fuentes (web‑intensive) del ensamblado y envío.
- **Acciones:**
  1. **Nuevo job `briefing_sources_fetch`** (schedule 07:50 America/Montevideo):
     - Usa subagente `researcher` para generar **solo secciones 6‑10** (world news, tech news, X links, historical fact, music discovery).
     - Guarda resultado en `/home/manpac/.openclaw/workspace/reports/briefing_sources/YYYY‑MM‑DD.md`.
     - No envía mensajes; finaliza con `NO_REPLY`.
  2. **Job existente `daily‑morning‑briefing‑telegram`** (ID `b8ea6237‑5df6‑41cf‑af52‑988abd5dd906`):
     - Mantiene schedule 08:00.
     - Lee al inicio solo: `briefing‑config.md`, `morning‑briefing‑template.md`, y el archivo de fuentes del día (si no existe, pone “Sin señal fresca hoy”).
     - Reduce lecturas iniciales (elimina `tasks.md`, `handoffs.md`, `agents.md`, `models.md`) para runtime <30s.
     - Opcionalmente pasa por subagente `writer` si sobra tiempo.
     - Envía a Telegram target definido en `briefing‑config.md`.
     - Appende logs a `handoffs.md` (1‑2 líneas).
- **Evidencia:** Instrucciones detalladas en memoria `2026‑02‑22‑0017.md`.
- **Impacto:** Mayor robustez, menor probabilidad de timeout, separación de responsabilidades.

### 3) Corrección de errores en cron jobs existentes
- **Fuente:** Error de cron el 2026‑02‑21 23:43 UTC (malformed `read_compact.sh` command).
- **Problema:** Comando truncado faltaba extensión `.md` y cierre de quote.
- **Solución:** Revisión y corrección manual de los comandos en `jobs.json`.
- **Evidencia:** Notificación de error en memoria `2026‑02‑22‑0017.md`.
- **Impacto:** Previene fallos en ejecuciones futuras.

### 4) Auditoría de rendimiento del gateway y ajuste de timeouts
- **Fuente:** Timeout de cliente (30s) mientras gateway respondía en 58s (ejecución manual de cron job `b8ea…`).
- **Análisis:** 
  - Gateway activo y saludable (PID 907830, memoria 493 MB).
  - Load average bajo (0.38, 0.23, 0.19), memoria libre 4.0 GB.
  - Cuellos de botella: 6 llamadas secuenciales a `read_compact.sh` + delegación a subagentes.
- **Decisiones:**
  - Optimizar cron job reduciendo lecturas (solo 2 archivos esenciales).
  - Considerar aumento de timeout del cliente si persisten límites.
- **Evidencia:** Auditoría completa en memoria `2026‑02‑22‑0017.md` (sección “📊 Auditoría del Gateway & Cron Job”).
- **Impacto:** Identificación de puntos de mejora para garantizar ejecución confiable.

### 5) Actualización de jobs cron para usar wrappers compactadores
- **Fuente:** Tarea de compactación de tokens (2026‑02‑21 23:00 UTC).
- **Jobs modificados:**
  1. `b8ea6237‑5df6‑41cf‑af52‑988abd5dd906` (daily‑morning‑briefing‑telegram):
     - Reemplazadas lecturas directas por `read_compact.sh "<ruta>"`.
  2. `f7ba2a8c‑6339‑4041‑bac9‑7e29f3f92383` (daily‑research‑scout):
     - Envolvió `search‑searx` con `exec_compact.sh`.
  3. `6ed083c2‑ac56‑42dc‑a18d‑7ecf4d41ae7f` (nightly_summary):
     - Envolvió comando Python con `exec_compact.sh`.
- **Evidencia:** Diff de cambios en memoria `2026‑02‑22‑0017.md` (sección “File edited”).
- **Impacto:** Reducción esperada de tokens inyectados en cada ejecución.

### 6) Definición de mejoras estructurales futuras
- **Fuente:** Plan de optimización presentado el 2026‑02‑21 19:32 UTC.
- **Componentes:**
  - **Tool Output Compaction:** Extender a `read`, `process`, `cron`.
  - **Session Rotation:** Monitoreo de msg count (60), duración (90 min), tool calls (40); checkpoint + restart.
  - **Cron Cost Guard:** Limitar loops a 2 iteraciones, 1 retry, fallback a partial result.
  - **Validación:** Medir avg tokens/msg antes/después, estimar reducción diaria.
- **Evidencia:** Memoria `2026‑02‑21‑2035.md`.
- **Impacto:** Hoja de ruta para sostenibilidad a largo plazo del sistema.

## Decisiones operativas del día
1. **Prioridad MVP compactación:** Se decidió implementar primero Tool Output Compaction para `exec` como skill‑level wrapper, sin modificar core de OpenClaw.
2. **Split del morning briefing:** Se adoptó arquitectura de dos etapas (fetch + assemble) para mejorar robustez y evitar timeouts.
3. **Umbrales de compactación:** Se establecieron >150 líneas **o** >10k caracteres como triggers para guardar raw y generar resumen.
4. **Mantenimiento de jobs existentes:** Se actualizaron cron jobs críticos para usar wrappers compactadores, priorizando reducción de tokens.
5. **Monitorización de rendimiento:** Se acordó monitorear tiempos de ejecución tras optimizaciones y ajustar timeouts si es necesario.

## Estado al cierre
- **Cambios relevantes confirmados:** Sí, hubo modificaciones significativas en automatizaciones (MVP compactación, reestructuración briefing, corrección de errores, auditoría gateway).
- **Configuraciones productivas:** 
  - Skill `exec‑compact` listo y testeado.
  - Cron jobs modificados (`daily‑morning‑briefing‑telegram`, `daily‑research‑scout`, `nightly_summary`).
  - Nuevo job `briefing_sources_fetch` pendiente de creación (ID por definir).
- **Documentación:** Esta bitácora diaria; el documento maestro no fue sobrescrito.
- **Métricas pendientes:** Validación de reducción de avg tokens/msg tras integración completa.

## Cambios pendientes de revisión humana
- Crear job `briefing_sources_fetch` con ID definitivo y schedule 07:50 America/Montevideo.
- Validar que el split del morning briefing funcione correctamente en ejecución real (prueba dry‑run de ambos jobs).
- Revisar que los wrappers `exec_compact.sh` y `read_compact.sh` no introduzcan regresiones en otros flujos.
- Definir cronograma para extender Tool Output Compaction a `read`, `process`, `cron`.
- Aprobar plan de Session Rotation y Cron Cost Guard (priorizar después de validación MVP).